import type { BeeRequestOptions, ChainState, ReserveState } from '../types';
import type { BeeVersions, DebugStatus, Health, NodeInfo, Readiness } from '../types/debug';
import type { BeeContext } from './context';
/**
 * Node status, health, version and chain/reserve state operations.
 *
 * Accessed as `bee.status`.
 */
export declare class Status {
    private readonly context;
    constructor(context: BeeContext);
    /**
     * Gets the general status of the node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    get(requestOptions?: BeeRequestOptions): Promise<DebugStatus>;
    /**
     * Gets the health of the node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getHealth(requestOptions?: BeeRequestOptions): Promise<Health>;
    /**
     * Gets the readiness status of the node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getReadiness(requestOptions?: BeeRequestOptions): Promise<Readiness>;
    /**
     * Gets mode information of the node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getNodeInfo(requestOptions?: BeeRequestOptions): Promise<NodeInfo>;
    /**
     * Connects to a node and checks if its version matches with the one that bee-js supports.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    isSupportedExactVersion(requestOptions?: BeeRequestOptions): Promise<boolean>;
    /**
     * Connects to a node and checks if its Main API version matches with the one that bee-js supports.
     *
     * This should be the main way how to check compatibility for your app and Bee node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    isSupportedApiVersion(requestOptions?: BeeRequestOptions): Promise<boolean>;
    /**
     * Returns object with all versions specified by the connected Bee node (properties prefixed with `bee*`)
     * and versions that bee-js supports (properties prefixed with `supported*`).
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getVersions(requestOptions?: BeeRequestOptions): Promise<BeeVersions>;
    /**
     * Gets reserve state.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getReserveState(requestOptions?: BeeRequestOptions): Promise<ReserveState>;
    /**
     * Gets chain state.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getChainState(requestOptions?: BeeRequestOptions): Promise<ChainState>;
}
