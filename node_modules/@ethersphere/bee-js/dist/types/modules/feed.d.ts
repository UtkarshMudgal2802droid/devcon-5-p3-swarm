import { BatchId, EthAddress, FeedIndex, PrivateKey, Reference, Topic } from '@ethersphere/core-sdk';
import type { FeedPayloadResult } from '../api/feed';
import type { BeeRequestOptions, DownloadOptions, FeedReader, FeedWriter } from '../types';
import { UploadOptions } from '../types';
import type { BeeContext } from './context';
/**
 * Feed operations.
 *
 * Accessed as `bee.feed`. Delegates to the feed subsystem (`src/feed`).
 */
export declare class Feed {
    private readonly context;
    constructor(context: BeeContext);
    /**
     * Makes a new feed reader for downloading feed updates.
     *
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeReader(topic: Topic | Uint8Array | string, owner: EthAddress | Uint8Array | string, requestOptions?: BeeRequestOptions): FeedReader;
    /**
     * Makes a new feed writer for updating feeds.
     *
     * @param topic Topic in hex or bytes
     * @param signer The signer's private key. Falls back to the Bee instance signer.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeWriter(topic: Topic | Uint8Array | string, signer?: PrivateKey | Uint8Array | string, requestOptions?: BeeRequestOptions): FeedWriter;
    /**
     * Creates a feed manifest chunk and returns the reference to it.
     *
     * @param postageBatchId Postage BatchId to be used to create the Feed Manifest
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param options Options that affect the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    createManifest(postageBatchId: BatchId | Uint8Array | string, topic: Topic | Uint8Array | string, owner: EthAddress | Uint8Array | string, options?: UploadOptions, requestOptions?: BeeRequestOptions): Promise<Reference>;
    /**
     * Fetches the latest feed update.
     *
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    fetchLatestUpdate(topic: Topic | Uint8Array | string, owner: EthAddress | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<FeedPayloadResult>;
    /**
     * Validates whether a feed is retrievable in the network.
     *
     * @param owner Owner's ethereum address in hex or bytes
     * @param topic Topic in hex or bytes
     * @param index Optional feed index
     * @param options Download options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    isRetrievable(owner: EthAddress | Uint8Array | string, topic: Topic | Uint8Array | string, index?: FeedIndex, options?: DownloadOptions, requestOptions?: BeeRequestOptions): Promise<boolean>;
}
