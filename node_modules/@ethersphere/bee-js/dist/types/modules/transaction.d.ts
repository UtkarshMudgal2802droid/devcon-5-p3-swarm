import { TransactionId } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, NumberString, TransactionInfo } from '../types';
import type { BeeContext } from './context';
/**
 * Pending transaction operations for the Bee node's `/transactions` queue.
 *
 * Accessed as `bee.transaction`.
 */
export declare class Transaction {
    private readonly context;
    constructor(context: BeeContext);
    /**
     * Fetches the list of all current pending transactions for the Bee node.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getAll(requestOptions?: BeeRequestOptions): Promise<TransactionInfo[]>;
    /**
     * Fetches the transaction information for a specific pending transaction.
     *
     * @param transactionHash Hash of the transaction
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    get(transactionHash: TransactionId | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<TransactionInfo>;
    /**
     * Rebroadcasts an already created pending transaction.
     *
     * Mainly needed when the transaction falls off the mempool or is not incorporated into any block.
     *
     * @param transactionHash Hash of the transaction
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    rebroadcast(transactionHash: TransactionId | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<TransactionId>;
    /**
     * Cancels a currently pending transaction.
     *
     * @param transactionHash Hash of the transaction
     * @param gasPrice Optional gas price
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    cancel(transactionHash: TransactionId | Uint8Array | string, gasPrice?: NumberString | string | bigint, requestOptions?: BeeRequestOptions): Promise<TransactionId>;
}
