import { BatchId, Reference } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, Pin as PinData } from '../types';
import type { BeeContext } from './context';
/**
 * Local pinning operations.
 *
 * Accessed as `bee.pin`.
 */
export declare class Pin {
    private readonly context;
    constructor(context: BeeContext);
    /**
     * Pins local data with the given reference.
     *
     * @param reference Data reference
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    add(reference: Reference | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<void>;
    /**
     * Unpins local data with the given reference.
     *
     * @param reference Data reference
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    remove(reference: Reference | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<void>;
    /**
     * Gets the list of all locally pinned references.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getAll(requestOptions?: BeeRequestOptions): Promise<Reference[]>;
    /**
     * Gets the pinning status of the chunk with the given reference.
     *
     * @param reference Data reference
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    get(reference: Reference | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<PinData>;
    /**
     * Instructs the Bee node to reupload locally pinned data into the network.
     *
     * @param postageBatchId Postage Batch ID that will be used to re-upload the data.
     * @param reference Data reference to be re-uploaded.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    reuploadData(postageBatchId: BatchId | Uint8Array | string, reference: Reference | Uint8Array | string, requestOptions?: BeeRequestOptions): Promise<void>;
}
