import { PeerAddress, TransactionId } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, LastCashoutActionResponse, LastChequesForPeerResponse, LastChequesResponse, TransactionOptions } from '../types';
import type { BeeContext } from './context';
/**
 * Cheque operations (last cheques and cashouts).
 *
 * Accessed as `bee.cheque`.
 */
export declare class Cheque {
    private readonly context;
    constructor(context: BeeContext);
    /**
     * Gets the last cheques for all peers.
     *
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getAllLatest(requestOptions?: BeeRequestOptions): Promise<LastChequesResponse>;
    /**
     * Gets the last cheques for a specific peer.
     *
     * @param address Overlay address of peer.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getAllLatestForPeer(address: PeerAddress | string, requestOptions?: BeeRequestOptions): Promise<LastChequesForPeerResponse>;
    /**
     * Gets the last cashout action for a specific peer.
     *
     * @param address Overlay address of peer.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    getLastCashoutAction(address: PeerAddress | string, requestOptions?: BeeRequestOptions): Promise<LastCashoutActionResponse>;
    /**
     * Cashes out the last cheque for a specific peer.
     *
     * @param address Swarm address of peer
     * @param options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    cashoutLast(address: PeerAddress | string, options?: TransactionOptions, requestOptions?: BeeRequestOptions): Promise<TransactionId>;
}
