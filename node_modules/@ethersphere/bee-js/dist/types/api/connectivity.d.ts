import { PeerAddress } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, Peer, PingResponse, RemovePeerResponse, Topology } from '../types';
import { NodeAddresses } from '../types/debug';
/**
 * Raw HTTP calls for the connectivity endpoints (`/addresses`, `/peers`, `/topology`, `/blocklist`, `/pingpong`, etc.).
 */
/** Pings the Bee node to see if there is a live Bee node on the given URL. */
export declare function checkConnection(requestOptions: BeeRequestOptions): Promise<void>;
/** Checks the `/gateway` endpoint to see if the remote API is a gateway. */
export declare function isGateway(requestOptions: BeeRequestOptions): Promise<boolean>;
/** Fetches the overlay, underlay, Ethereum, and other addresses of the Bee node. */
export declare function getNodeAddresses(requestOptions: BeeRequestOptions): Promise<NodeAddresses>;
/** Fetches the list of blocked peers for this node. */
export declare function getBlocklist(requestOptions: BeeRequestOptions): Promise<Peer[]>;
/** Gets list of peers for this node. */
export declare function getPeers(requestOptions: BeeRequestOptions): Promise<Peer[]>;
/** Disconnects from a specific peer. */
export declare function removePeer(requestOptions: BeeRequestOptions, peer: PeerAddress): Promise<RemovePeerResponse>;
/** Fetches topology and connectivity information of the Bee node. */
export declare function getTopology(requestOptions: BeeRequestOptions): Promise<Topology>;
/** Pings a specific peer to check its availability. */
export declare function ping(requestOptions: BeeRequestOptions, peer: PeerAddress): Promise<PingResponse>;
