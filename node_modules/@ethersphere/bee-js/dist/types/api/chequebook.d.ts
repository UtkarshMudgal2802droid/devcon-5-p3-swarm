import { PeerAddress, TransactionId } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, ChequebookAddressResponse, ChequebookBalanceResponse, LastCashoutActionResponse, LastChequesForPeerResponse, LastChequesResponse, TransactionOptions } from '../types';
/**
 * Raw HTTP calls for the `/chequebook` endpoint (chequebook + cheque operations).
 */
/** Gets the address of the deployed chequebook. */
export declare function getChequebookAddress(requestOptions: BeeRequestOptions): Promise<ChequebookAddressResponse>;
/** Gets the balance of the chequebook. */
export declare function getChequebookBalance(requestOptions: BeeRequestOptions): Promise<ChequebookBalanceResponse>;
/** Deposits tokens from the node wallet into the chequebook. */
export declare function depositTokens(requestOptions: BeeRequestOptions, amount: string, gasPrice?: string): Promise<TransactionId>;
/** Withdraws tokens from the chequebook to the node wallet. */
export declare function withdrawTokens(requestOptions: BeeRequestOptions, amount: string, gasPrice?: string): Promise<TransactionId>;
/** Gets the last cheques for all peers. */
export declare function getLastCheques(requestOptions: BeeRequestOptions): Promise<LastChequesResponse>;
/** Gets the last cheques for a specific peer. */
export declare function getLastChequesForPeer(requestOptions: BeeRequestOptions, peer: PeerAddress): Promise<LastChequesForPeerResponse>;
/** Gets the last cashout action for a specific peer. */
export declare function getLastCashoutAction(requestOptions: BeeRequestOptions, peer: PeerAddress): Promise<LastCashoutActionResponse>;
/** Cashes out the last cheque for a specific peer. */
export declare function cashoutLastCheque(requestOptions: BeeRequestOptions, peer: PeerAddress, options?: TransactionOptions): Promise<TransactionId>;
