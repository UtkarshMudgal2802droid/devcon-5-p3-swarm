import { BatchId, PublicKey, Reference } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, GetGranteesResult, GranteesResult } from '../types';
/** Creates grantees for a postage batch. */
export declare function createGrantees(requestOptions: BeeRequestOptions, batchId: BatchId, publicKeys: PublicKey[]): Promise<GranteesResult>;
/** Retrieves the grantees for a given reference. */
export declare function getGrantees(requestOptions: BeeRequestOptions, reference: Reference): Promise<GetGranteesResult>;
/** Updates the grantees of a specific reference and history. */
export declare function patchGrantees(requestOptions: BeeRequestOptions, batchId: BatchId, reference: Reference, history: Reference, publicKeys: {
    add: PublicKey[];
    revoke: PublicKey[];
}): Promise<GranteesResult>;
