import { PeerAddress } from '@ethersphere/core-sdk';
import type { BalanceResponse, BeeRequestOptions, PeerBalance } from '../types';
/**
 * Raw HTTP calls for the `/balances` endpoint.
 */
export declare function getBalances(requestOptions: BeeRequestOptions): Promise<BalanceResponse>;
export declare function getPeerBalance(requestOptions: BeeRequestOptions, peer: PeerAddress): Promise<PeerBalance>;
