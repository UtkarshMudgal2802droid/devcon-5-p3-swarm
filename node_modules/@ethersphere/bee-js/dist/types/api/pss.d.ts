import { BatchId, PublicKey, Topic } from '@ethersphere/core-sdk';
import type { BeeRequestOptions } from '../types';
/**
 * Sends a message with the Postal Service for Swarm through the `POST /pss/send` endpoint.
 */
export declare function send(requestOptions: BeeRequestOptions, batchId: BatchId, topic: Topic, target: string, data: string | Uint8Array, recipient?: PublicKey): Promise<void>;
