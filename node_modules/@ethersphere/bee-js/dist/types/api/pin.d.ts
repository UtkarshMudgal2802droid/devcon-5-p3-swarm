import { Reference } from '@ethersphere/core-sdk';
import type { BeeRequestOptions, Pin as PinData } from '../types';
/** Pins local data with the given reference. */
export declare function pin(requestOptions: BeeRequestOptions, reference: Reference): Promise<void>;
/** Unpins local data with the given reference. */
export declare function unpin(requestOptions: BeeRequestOptions, reference: Reference): Promise<void>;
/** Gets the list of all locally pinned references. */
export declare function getAllPins(requestOptions: BeeRequestOptions): Promise<Reference[]>;
/** Gets the pinning status of the chunk with the given reference. */
export declare function getPin(requestOptions: BeeRequestOptions, reference: Reference): Promise<PinData>;
