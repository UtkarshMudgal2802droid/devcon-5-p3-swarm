import { BatchId, EthAddress, PrivateKey, Topic } from '@ethersphere/core-sdk';
import { Objects } from 'cafe-utility';
import { createFeedManifest, fetchLatestFeedUpdate } from "../api/feed.js";
import { makeFeedReader, makeFeedWriter } from "../feed/index.js";
import { areAllSequentialFeedsUpdateRetrievable } from "../feed/retrievable.js";
import { DownloadOptionsSchema, UploadOptionsSchema } from "../utils/schema.js";
/**
 * Feed operations.
 *
 * Accessed as `bee.feed`. Delegates to the feed subsystem (`src/feed`).
 */
export class Feed {
  constructor(context) {
    this.context = context;
  }
  /**
   * Makes a new feed reader for downloading feed updates.
   *
   * @param topic Topic in hex or bytes
   * @param owner Owner's ethereum address in hex or bytes
   * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
   */
  makeReader(topic, owner, requestOptions) {
    return makeFeedReader(this.context.getRequestOptionsForCall(requestOptions), new Topic(topic), new EthAddress(owner));
  }
  /**
   * Makes a new feed writer for updating feeds.
   *
   * @param topic Topic in hex or bytes
   * @param signer The signer's private key. Falls back to the Bee instance signer.
   * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
   */
  makeWriter(topic, signer, requestOptions) {
    const key = signer ? new PrivateKey(signer) : this.context.signer;
    if (!key) {
      throw Error('No signer provided');
    }
    return makeFeedWriter(this.context.getRequestOptionsForCall(requestOptions), new Topic(topic), key);
  }
  /**
   * Creates a feed manifest chunk and returns the reference to it.
   *
   * @param postageBatchId Postage BatchId to be used to create the Feed Manifest
   * @param topic Topic in hex or bytes
   * @param owner Owner's ethereum address in hex or bytes
   * @param options Options that affect the request behavior
   * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
   */
  async createManifest(postageBatchId, topic, owner, options, requestOptions) {
    const batchId = new BatchId(postageBatchId);
    const feedTopic = new Topic(topic);
    const feedOwner = new EthAddress(owner);
    if (options) {
      options = UploadOptionsSchema.parse(options);
    }
    return createFeedManifest(this.context.getRequestOptionsForCall(requestOptions), feedOwner, feedTopic, batchId, options);
  }
  /**
   * Fetches the latest feed update.
   *
   * @param topic Topic in hex or bytes
   * @param owner Owner's ethereum address in hex or bytes
   * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
   */
  async fetchLatestUpdate(topic, owner, requestOptions) {
    return fetchLatestFeedUpdate(this.context.getRequestOptionsForCall(requestOptions), new EthAddress(owner), new Topic(topic));
  }
  /**
   * Validates whether a feed is retrievable in the network.
   *
   * @param owner Owner's ethereum address in hex or bytes
   * @param topic Topic in hex or bytes
   * @param index Optional feed index
   * @param options Download options
   * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
   */
  async isRetrievable(owner, topic, index, options, requestOptions) {
    const feedOwner = new EthAddress(owner);
    const feedTopic = new Topic(topic);
    if (options) {
      options = DownloadOptionsSchema.parse(options);
    }
    if (!index) {
      try {
        await this.makeReader(feedTopic, feedOwner, requestOptions).download();
        return true;
      } catch (e) {
        const status = Objects.getDeep(e, 'status');
        if (status === 404 || status === 500) {
          return false;
        }
        throw e;
      }
    }
    return areAllSequentialFeedsUpdateRetrievable(this.context.bee, feedOwner, feedTopic, index, options, this.context.getRequestOptionsForCall(requestOptions));
  }
}