import { GetDebugStatusResponse, GetHealthResponse, GetNodeInfoResponse, GetReadinessResponse } from "../types/schema/status.js";
import { GetChainStateResponse, GetReserveStateResponse } from "../types/schema/states.js";
import { http } from "../utils/http.js";
const NODE_INFO_URL = 'node';
const STATUS_URL = 'status';
const HEALTH_URL = 'health';
const READINESS_URL = 'readiness';
const RESERVE_STATE_ENDPOINT = 'reservestate';
const CHAIN_STATE_ENDPOINT = 'chainstate';
/**
 * Raw HTTP calls for the node status, health, version and chain/reserve state endpoints.
 */
export async function getDebugStatus(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: STATUS_URL,
    responseType: 'json'
  });
  return GetDebugStatusResponse.parse(response.data);
}
export async function getHealth(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: HEALTH_URL,
    responseType: 'json'
  });
  return GetHealthResponse.parse(response.data);
}
export async function getReadiness(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: READINESS_URL
  });
  return GetReadinessResponse.parse(response.data);
}
export async function getNodeInfo(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: NODE_INFO_URL,
    responseType: 'json'
  });
  return GetNodeInfoResponse.parse(response.data);
}
export async function getReserveState(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: RESERVE_STATE_ENDPOINT,
    responseType: 'json'
  });
  return GetReserveStateResponse.parse(response.data);
}
export async function getChainState(requestOptions) {
  const response = await http(requestOptions, {
    method: 'get',
    url: CHAIN_STATE_ENDPOINT,
    responseType: 'json'
  });
  return GetChainStateResponse.parse(response.data);
}