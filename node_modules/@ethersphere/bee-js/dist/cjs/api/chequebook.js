"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getChequebookAddress = getChequebookAddress;
exports.getChequebookBalance = getChequebookBalance;
exports.depositTokens = depositTokens;
exports.withdrawTokens = withdrawTokens;
exports.getLastCheques = getLastCheques;
exports.getLastChequesForPeer = getLastChequesForPeer;
exports.getLastCashoutAction = getLastCashoutAction;
exports.cashoutLastCheque = cashoutLastCheque;
const chequebook_1 = require("../types/schema/chequebook");
const headers_1 = require("../utils/headers");
const http_1 = require("../utils/http");
const chequebookEndpoint = 'chequebook';
/**
 * Raw HTTP calls for the `/chequebook` endpoint (chequebook + cheque operations).
 */
/** Gets the address of the deployed chequebook. */
async function getChequebookAddress(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${chequebookEndpoint}/address`,
        responseType: 'json',
    });
    return chequebook_1.GetChequebookAddressResponse.parse(response.data);
}
/** Gets the balance of the chequebook. */
async function getChequebookBalance(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${chequebookEndpoint}/balance`,
        responseType: 'json',
    });
    return chequebook_1.GetChequebookBalanceResponse.parse(response.data);
}
/** Deposits tokens from the node wallet into the chequebook. */
async function depositTokens(requestOptions, amount, gasPrice) {
    const headers = {};
    if (gasPrice) {
        headers['gas-price'] = gasPrice;
    }
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${chequebookEndpoint}/deposit`,
        responseType: 'json',
        params: { amount },
        headers,
    });
    return chequebook_1.TransactionHashResponse.parse(response.data).transactionHash;
}
/** Withdraws tokens from the chequebook to the node wallet. */
async function withdrawTokens(requestOptions, amount, gasPrice) {
    const headers = {};
    if (gasPrice) {
        headers['gas-price'] = gasPrice;
    }
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${chequebookEndpoint}/withdraw`,
        responseType: 'json',
        params: { amount },
        headers,
    });
    return chequebook_1.TransactionHashResponse.parse(response.data).transactionHash;
}
/** Gets the last cheques for all peers. */
async function getLastCheques(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${chequebookEndpoint}/cheque`,
        responseType: 'json',
    });
    return chequebook_1.GetLastChequesResponse.parse(response.data);
}
/** Gets the last cheques for a specific peer. */
async function getLastChequesForPeer(requestOptions, peer) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${chequebookEndpoint}/cheque/${peer}`,
        responseType: 'json',
    });
    return chequebook_1.GetLastChequesForPeerResponse.parse(response.data);
}
/** Gets the last cashout action for a specific peer. */
async function getLastCashoutAction(requestOptions, peer) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${chequebookEndpoint}/cashout/${peer}`,
        responseType: 'json',
    });
    return chequebook_1.GetLastCashoutActionResponse.parse(response.data);
}
/** Cashes out the last cheque for a specific peer. */
async function cashoutLastCheque(requestOptions, peer, options) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${chequebookEndpoint}/cashout/${peer}`,
        responseType: 'json',
        headers: (0, headers_1.prepareRequestHeaders)(null, options),
    });
    return chequebook_1.TransactionHashResponse.parse(response.data).transactionHash;
}
