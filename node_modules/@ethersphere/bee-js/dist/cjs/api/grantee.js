"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createGrantees = createGrantees;
exports.getGrantees = getGrantees;
exports.patchGrantees = patchGrantees;
const grantee_1 = require("../types/schema/grantee");
const headers_1 = require("../utils/headers");
const http_1 = require("../utils/http");
const granteeEndpoint = 'grantee';
/** Creates grantees for a postage batch. */
async function createGrantees(requestOptions, batchId, publicKeys) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: granteeEndpoint,
        data: { grantees: publicKeys.map(x => x.toCompressedHex()) },
        headers: (0, headers_1.prepareRequestHeaders)(batchId),
        responseType: 'json',
    });
    const body = grantee_1.GranteesResultBodyResponse.parse(response.data);
    return {
        status: response.status,
        statusText: response.statusText,
        ref: body.ref,
        historyref: body.historyref,
    };
}
/** Retrieves the grantees for a given reference. */
async function getGrantees(requestOptions, reference) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        url: `${granteeEndpoint}/${reference}`,
        responseType: 'json',
    });
    return {
        status: response.status,
        statusText: response.statusText,
        grantees: grantee_1.GetGranteesBodyResponse.parse(response.data),
    };
}
/** Updates the grantees of a specific reference and history. */
async function patchGrantees(requestOptions, batchId, reference, history, publicKeys) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'patch',
        url: `${granteeEndpoint}/${reference}`,
        data: {
            add: publicKeys.add.map(x => x.toCompressedHex()),
            revoke: publicKeys.revoke.map(x => x.toCompressedHex()),
        },
        headers: {
            ...(0, headers_1.prepareRequestHeaders)(batchId),
            'swarm-act-history-address': history.toHex(),
        },
        responseType: 'json',
    });
    const body = grantee_1.GranteesResultBodyResponse.parse(response.data);
    return {
        status: response.status,
        statusText: response.statusText,
        ref: body.ref,
        historyref: body.historyref,
    };
}
