"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkConnection = checkConnection;
exports.isGateway = isGateway;
exports.getNodeAddresses = getNodeAddresses;
exports.getBlocklist = getBlocklist;
exports.getPeers = getPeers;
exports.removePeer = removePeer;
exports.getTopology = getTopology;
exports.ping = ping;
const connectivity_1 = require("../types/schema/connectivity");
const status_1 = require("../types/schema/status");
const http_1 = require("../utils/http");
const addressesEndpoint = 'addresses';
const blocklistEndpoint = 'blocklist';
const peersEndpoint = 'peers';
const topologyEndpoint = 'topology';
const pingpongEndpoint = 'pingpong';
const gatewayEndpoint = '/gateway';
/**
 * Raw HTTP calls for the connectivity endpoints (`/addresses`, `/peers`, `/topology`, `/blocklist`, `/pingpong`, etc.).
 */
/** Pings the Bee node to see if there is a live Bee node on the given URL. */
async function checkConnection(requestOptions) {
    await (0, http_1.http)(requestOptions, {
        url: '',
        responseType: 'text',
    });
}
/** Checks the `/gateway` endpoint to see if the remote API is a gateway. */
async function isGateway(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: gatewayEndpoint,
    });
    return status_1.IsGatewayResponse.parse(response.data).gateway;
}
/** Fetches the overlay, underlay, Ethereum, and other addresses of the Bee node. */
async function getNodeAddresses(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: addressesEndpoint,
        responseType: 'json',
    });
    return connectivity_1.GetNodeAddressesResponse.parse(response.data);
}
/** Fetches the list of blocked peers for this node. */
async function getBlocklist(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: blocklistEndpoint,
        responseType: 'json',
    });
    return connectivity_1.GetBlocklistResponse.parse(response.data).peers;
}
/** Gets list of peers for this node. */
async function getPeers(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: peersEndpoint,
        responseType: 'json',
    });
    return connectivity_1.GetPeersResponse.parse(response.data).peers;
}
/** Disconnects from a specific peer. */
async function removePeer(requestOptions, peer) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${peersEndpoint}/${peer}`,
        responseType: 'json',
        method: 'DELETE',
    });
    return response.data;
}
/** Fetches topology and connectivity information of the Bee node. */
async function getTopology(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: topologyEndpoint,
        responseType: 'json',
    });
    return connectivity_1.GetTopologyResponse.parse(response.data);
}
/** Pings a specific peer to check its availability. */
async function ping(requestOptions, peer) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${pingpongEndpoint}/${peer}`,
        responseType: 'json',
        method: 'POST',
    });
    return response.data;
}
