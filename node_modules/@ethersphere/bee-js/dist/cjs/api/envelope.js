"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.postEnvelope = postEnvelope;
const core_sdk_1 = require("@ethersphere/core-sdk");
const envelope_1 = require("../types/schema/envelope");
const http_1 = require("../utils/http");
const ENVELOPE_ENDPOINT = 'envelope';
async function postEnvelope(requestOptions, postageBatchId, reference) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        responseType: 'json',
        url: `${ENVELOPE_ENDPOINT}/${reference}`,
        headers: {
            'swarm-postage-batch-id': postageBatchId.toHex(),
        },
    });
    const body = envelope_1.PostEnvelopeBodyResponse.parse(response.data);
    return {
        issuer: new core_sdk_1.EthAddress(body.issuer),
        index: body.index,
        timestamp: body.timestamp,
        signature: new core_sdk_1.Signature(body.signature),
        batchId: postageBatchId,
    };
}
