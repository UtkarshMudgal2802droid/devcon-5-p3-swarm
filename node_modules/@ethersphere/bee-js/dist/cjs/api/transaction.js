"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAllTransactions = getAllTransactions;
exports.getTransaction = getTransaction;
exports.rebroadcastTransaction = rebroadcastTransaction;
exports.cancelTransaction = cancelTransaction;
const transactions_1 = require("../types/schema/transactions");
const http_1 = require("../utils/http");
const transactionsEndpoint = 'transactions';
/**
 * Raw HTTP calls for the `/transactions` endpoint.
 */
async function getAllTransactions(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        url: transactionsEndpoint,
        responseType: 'json',
    });
    return transactions_1.GetAllTransactionsResponse.parse(response.data).pendingTransactions;
}
async function getTransaction(requestOptions, hash) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${transactionsEndpoint}/${hash}`,
        responseType: 'json',
    });
    return transactions_1.GetTransactionResponse.parse(response.data);
}
async function rebroadcastTransaction(requestOptions, hash) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${transactionsEndpoint}/${hash}`,
        responseType: 'json',
    });
    return transactions_1.TransactionHashResponse.parse(response.data).transactionHash;
}
async function cancelTransaction(requestOptions, hash, gasPrice) {
    const headers = {};
    if (gasPrice) {
        headers['gas-price'] = gasPrice;
    }
    const response = await (0, http_1.http)(requestOptions, {
        method: 'delete',
        headers,
        url: `${transactionsEndpoint}/${hash}`,
        responseType: 'json',
    });
    return transactions_1.TransactionHashResponse.parse(response.data).transactionHash;
}
