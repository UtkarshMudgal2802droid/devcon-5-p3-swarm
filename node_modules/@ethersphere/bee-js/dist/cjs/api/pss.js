"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.send = send;
const headers_1 = require("../utils/headers");
const http_1 = require("../utils/http");
const pssEndpoint = 'pss';
/**
 * Sends a message with the Postal Service for Swarm through the `POST /pss/send` endpoint.
 */
async function send(requestOptions, batchId, topic, target, data, recipient) {
    await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${pssEndpoint}/send/${topic}/${target}`,
        data,
        responseType: 'json',
        params: { recipient },
        headers: (0, headers_1.prepareRequestHeaders)(batchId),
    });
}
