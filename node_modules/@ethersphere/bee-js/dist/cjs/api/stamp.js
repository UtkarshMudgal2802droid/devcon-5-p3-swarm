"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPostageBatch = createPostageBatch;
exports.updateLabel = updateLabel;
exports.topUpBatch = topUpBatch;
exports.diluteBatch = diluteBatch;
exports.getPostageBatch = getPostageBatch;
exports.getPostageBatchBuckets = getPostageBatchBuckets;
exports.getAllPostageBatches = getAllPostageBatches;
const stamps_1 = require("../types/schema/stamps");
const http_1 = require("../utils/http");
const stamps_2 = require("../utils/stamps");
const STAMPS_ENDPOINT = 'stamps';
/**
 * Raw HTTP calls for the `/stamps` endpoint.
 */
async function createPostageBatch(requestOptions, amount, depth, label, gasPrice, immutableFlag) {
    const headers = {};
    if (gasPrice) {
        headers['gas-price'] = gasPrice;
    }
    if (immutableFlag !== undefined) {
        headers.immutable = String(immutableFlag);
    }
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: `${STAMPS_ENDPOINT}/${amount}/${depth}`,
        responseType: 'json',
        params: { label },
        headers,
    });
    return stamps_1.BatchIdResponse.parse(response.data).batchID;
}
async function updateLabel(requestOptions, id, label) {
    await (0, http_1.http)(requestOptions, {
        method: 'patch',
        url: `${STAMPS_ENDPOINT}/${id}`,
        responseType: 'json',
        data: { label },
    });
}
async function topUpBatch(requestOptions, id, amount) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'patch',
        url: `${STAMPS_ENDPOINT}/topup/${id}/${amount}`,
        responseType: 'json',
    });
    return stamps_1.BatchIdResponse.parse(response.data).batchID;
}
async function diluteBatch(requestOptions, id, depth) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'patch',
        url: `${STAMPS_ENDPOINT}/dilute/${id}/${depth}`,
        responseType: 'json',
    });
    return stamps_1.BatchIdResponse.parse(response.data).batchID;
}
async function getPostageBatch(requestOptions, id, encryption, erasureCodeLevel) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        url: `${STAMPS_ENDPOINT}/${id}`,
        responseType: 'json',
    });
    return (0, stamps_2.mapPostageBatch)(stamps_1.GetPostageBatchResponse.parse(response.data), encryption, erasureCodeLevel);
}
async function getPostageBatchBuckets(requestOptions, id) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        url: `${STAMPS_ENDPOINT}/${id}/buckets`,
        responseType: 'json',
    });
    return stamps_1.GetPostageBatchBucketsResponse.parse(response.data);
}
async function getAllPostageBatches(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        url: STAMPS_ENDPOINT,
        responseType: 'json',
    });
    return stamps_1.GetAllPostageBatchesResponse.parse(response.data).stamps.map(x => (0, stamps_2.mapPostageBatch)(x));
}
