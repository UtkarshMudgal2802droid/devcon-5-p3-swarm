"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pin = pin;
exports.unpin = unpin;
exports.getAllPins = getAllPins;
exports.getPin = getPin;
const core_sdk_1 = require("@ethersphere/core-sdk");
const pinning_1 = require("../types/schema/pinning");
const upload_1 = require("../types/schema/upload");
const http_1 = require("../utils/http");
const PINNING_ENDPOINT = 'pins';
/** Pins local data with the given reference. */
async function pin(requestOptions, reference) {
    await (0, http_1.http)(requestOptions, {
        method: 'post',
        responseType: 'json',
        url: `${PINNING_ENDPOINT}/${reference}`,
    });
}
/** Unpins local data with the given reference. */
async function unpin(requestOptions, reference) {
    await (0, http_1.http)(requestOptions, {
        method: 'delete',
        responseType: 'json',
        url: `${PINNING_ENDPOINT}/${reference}`,
    });
}
/** Gets the list of all locally pinned references. */
async function getAllPins(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        responseType: 'json',
        url: PINNING_ENDPOINT,
    });
    const { references } = pinning_1.GetAllPinsResponse.parse(response.data);
    return (references ?? []).map(x => new core_sdk_1.Reference(x));
}
/** Gets the pinning status of the chunk with the given reference. */
async function getPin(requestOptions, reference) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'get',
        responseType: 'json',
        url: `${PINNING_ENDPOINT}/${reference}`,
    });
    return upload_1.UploadResultBody.parse(response.data);
}
