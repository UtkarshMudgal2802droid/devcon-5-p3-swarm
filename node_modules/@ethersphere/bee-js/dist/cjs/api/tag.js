"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTag = createTag;
exports.getAllTags = getAllTags;
exports.getTag = getTag;
exports.deleteTag = deleteTag;
exports.updateTag = updateTag;
const tag_1 = require("../types/schema/tag");
const http_1 = require("../utils/http");
const endpoint = 'tags';
/** Creates a new tag. */
async function createTag(requestOptions) {
    const response = await (0, http_1.http)(requestOptions, {
        method: 'post',
        url: endpoint,
        responseType: 'json',
    });
    return tag_1.TagSchema.parse(response.data);
}
/** Fetches all tags in a paginated manner. */
async function getAllTags(requestOptions, options) {
    const response = await (0, http_1.http)(requestOptions, {
        url: endpoint,
        params: { offset: options?.offset, limit: options?.limit },
        responseType: 'json',
    });
    return tag_1.GetAllTagsResponse.parse(response.data).tags;
}
/** Retrieves tag information from the Bee node. */
async function getTag(requestOptions, uid) {
    const response = await (0, http_1.http)(requestOptions, {
        url: `${endpoint}/${uid}`,
        responseType: 'json',
    });
    return tag_1.TagSchema.parse(response.data);
}
/** Deletes a tag. */
async function deleteTag(requestOptions, uid) {
    await (0, http_1.http)(requestOptions, {
        method: 'delete',
        url: `${endpoint}/${uid}`,
    });
}
/** Updates a tag's total chunks count. */
async function updateTag(requestOptions, uid, reference) {
    await (0, http_1.http)(requestOptions, {
        method: 'patch',
        url: `${endpoint}/${uid}`,
        data: { reference },
    });
}
