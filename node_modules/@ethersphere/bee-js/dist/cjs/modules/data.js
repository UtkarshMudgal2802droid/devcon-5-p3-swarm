"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Data = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const bytes = __importStar(require("../api/bytes"));
const stewardship = __importStar(require("../api/stewardship"));
const resource_locator_1 = require("../utils/resource-locator");
const schema_1 = require("../utils/schema");
const type_1 = require("../utils/type");
/**
 * Raw data operations backed by the `/bytes` endpoint.
 *
 * Accessed as `bee.data`.
 */
class Data {
    constructor(context) {
        this.context = context;
    }
    /**
     * Uploads raw data through the `POST /bytes` endpoint.
     *
     * Data uploaded with this method should be retrieved with {@link download}.
     *
     * @param postageBatchId Usable Postage Batch ID with sufficient capacity to upload the data.
     * @param data           A `string` (text data) or `Uint8Array` (raw data) to be uploaded.
     * @param options        Additional options like tag, encryption, pinning, content-type and request options.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async upload(postageBatchId, data, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        (0, type_1.assertData)(data);
        if (options) {
            options = schema_1.RedundantUploadOptionsSchema.parse(options);
        }
        return bytes.upload(this.context.getRequestOptionsForCall(requestOptions), data, batchId, options);
    }
    /**
     * Downloads raw data through the `GET /bytes/{reference}` endpoint.
     *
     * @param resource Swarm reference, Swarm CID, or ENS domain.
     * @param options Options that affects the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async download(resource, options, requestOptions) {
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        return bytes.download(this.context.getRequestOptionsForCall(requestOptions), new resource_locator_1.ResourceLocator(resource), options);
    }
    /**
     * Downloads raw data through the `GET /bytes/{reference}` endpoint as a readable stream.
     *
     * Only tested in Node.js environment.
     *
     * @param resource Swarm reference, Swarm CID, or ENS domain.
     * @param options Options that affects the request behavior.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async downloadReadable(resource, options, requestOptions) {
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        return bytes.downloadReadable(this.context.getRequestOptionsForCall(requestOptions), new resource_locator_1.ResourceLocator(resource), options);
    }
    /**
     * Fetches content length for a `/bytes` reference through the `HEAD /bytes/{reference}` endpoint.
     *
     * @param reference
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async probe(reference, requestOptions) {
        const ref = new core_sdk_1.Reference(reference);
        return bytes.head(this.context.getRequestOptionsForCall(requestOptions), ref);
    }
    /**
     * Checks if content specified by reference is retrievable from the network.
     *
     * @param reference Bee reference to be checked in hex string (either 64 or 128 chars long) or ENS domain.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async isRetrievable(reference, requestOptions) {
        const ref = new core_sdk_1.Reference(reference);
        return stewardship.isRetrievable(this.context.getRequestOptionsForCall(requestOptions), ref);
    }
}
exports.Data = Data;
