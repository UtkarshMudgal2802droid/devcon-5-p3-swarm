"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Soc = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const soc_1 = require("../chunk/soc");
/**
 * Single owner chunk (SOC) reader/writer operations.
 *
 * Accessed as `bee.soc`.
 */
class Soc {
    constructor(context) {
        this.context = context;
    }
    /**
     * Returns an object for reading single owner chunks.
     *
     * @param ownerAddress The ethereum address of the owner
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeReader(ownerAddress, requestOptions) {
        const owner = new core_sdk_1.EthAddress(ownerAddress);
        return {
            owner,
            download: soc_1.downloadSingleOwnerChunk.bind(null, this.context.getRequestOptionsForCall(requestOptions), owner),
        };
    }
    /**
     * Returns an object for reading and writing single owner chunks.
     *
     * @param signer The signer's private key. Falls back to the Bee instance signer.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeWriter(signer, requestOptions) {
        const key = signer ? new core_sdk_1.PrivateKey(signer) : this.context.signer;
        if (!key) {
            throw Error('No signer provided');
        }
        return {
            ...this.makeReader(key.publicKey().address(), requestOptions),
            upload: soc_1.uploadSingleOwnerChunkData.bind(null, this.context.getRequestOptionsForCall(requestOptions), key),
        };
    }
}
exports.Soc = Soc;
