"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.File = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const file_1 = require("../utils/file");
const resource_locator_1 = require("../utils/resource-locator");
const schema_1 = require("../utils/schema");
const type_1 = require("../utils/type");
const api = __importStar(require("../api/bzz"));
/**
 * Single-file operations backed by the `/bzz` endpoint.
 *
 * Accessed as `bee.file`.
 */
class File {
    constructor(context) {
        this.context = context;
    }
    /**
     * Uploads a single file to a Bee node.
     *
     * To download the file, use {@link download}.
     *
     * @param postageBatchId Postage BatchId to be used to upload the data with
     * @param data    Data or file to be uploaded
     * @param name    Optional name of the uploaded file
     * @param options Additional options like tag, encryption, pinning, content-type and request options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async upload(postageBatchId, data, name, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        (0, type_1.assertFileData)(data);
        if (options) {
            options = schema_1.FileUploadOptionsSchema.parse(options);
        }
        if (name && typeof name !== 'string') {
            throw new TypeError('name has to be string or undefined!');
        }
        if ((0, file_1.isFile)(data)) {
            const fileData = await (0, file_1.fileArrayBuffer)(data);
            const fileName = name ?? data.name;
            const contentType = data.type;
            const fileOptions = { ...options, contentType };
            return api.uploadFile(this.context.getRequestOptionsForCall(requestOptions), fileData, batchId, fileName, fileOptions);
        }
        else {
            return api.uploadFile(this.context.getRequestOptionsForCall(requestOptions), data, batchId, name, options);
        }
    }
    /**
     * Downloads a single file.
     *
     * @param resource Swarm reference, Swarm CID, or ENS domain.
     * @param path If reference points to manifest, then this parameter defines path to the file
     * @param options Options that affects the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async download(resource, path = '', options, requestOptions) {
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        return api.downloadFile(this.context.getRequestOptionsForCall(requestOptions), new resource_locator_1.ResourceLocator(resource), path, options);
    }
    /**
     * Downloads a single file as a readable stream.
     *
     * @param reference Bee file reference in hex string (either 64 or 128 chars long), ENS domain or Swarm CID.
     * @param path If reference points to manifest / collections, then this parameter defines path to the file
     * @param options Options that affects the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async downloadReadable(reference, path = '', options, requestOptions) {
        const ref = new core_sdk_1.Reference(reference);
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        return api.downloadFileReadable(this.context.getRequestOptionsForCall(requestOptions), ref, path, options);
    }
}
exports.File = File;
