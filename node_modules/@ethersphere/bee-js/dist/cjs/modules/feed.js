"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Feed = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const cafe_utility_1 = require("cafe-utility");
const feed_1 = require("../api/feed");
const feed_2 = require("../feed");
const retrievable_1 = require("../feed/retrievable");
const schema_1 = require("../utils/schema");
/**
 * Feed operations.
 *
 * Accessed as `bee.feed`. Delegates to the feed subsystem (`src/feed`).
 */
class Feed {
    constructor(context) {
        this.context = context;
    }
    /**
     * Makes a new feed reader for downloading feed updates.
     *
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeReader(topic, owner, requestOptions) {
        return (0, feed_2.makeFeedReader)(this.context.getRequestOptionsForCall(requestOptions), new core_sdk_1.Topic(topic), new core_sdk_1.EthAddress(owner));
    }
    /**
     * Makes a new feed writer for updating feeds.
     *
     * @param topic Topic in hex or bytes
     * @param signer The signer's private key. Falls back to the Bee instance signer.
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    makeWriter(topic, signer, requestOptions) {
        const key = signer ? new core_sdk_1.PrivateKey(signer) : this.context.signer;
        if (!key) {
            throw Error('No signer provided');
        }
        return (0, feed_2.makeFeedWriter)(this.context.getRequestOptionsForCall(requestOptions), new core_sdk_1.Topic(topic), key);
    }
    /**
     * Creates a feed manifest chunk and returns the reference to it.
     *
     * @param postageBatchId Postage BatchId to be used to create the Feed Manifest
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param options Options that affect the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async createManifest(postageBatchId, topic, owner, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        const feedTopic = new core_sdk_1.Topic(topic);
        const feedOwner = new core_sdk_1.EthAddress(owner);
        if (options) {
            options = schema_1.UploadOptionsSchema.parse(options);
        }
        return (0, feed_1.createFeedManifest)(this.context.getRequestOptionsForCall(requestOptions), feedOwner, feedTopic, batchId, options);
    }
    /**
     * Fetches the latest feed update.
     *
     * @param topic Topic in hex or bytes
     * @param owner Owner's ethereum address in hex or bytes
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async fetchLatestUpdate(topic, owner, requestOptions) {
        return (0, feed_1.fetchLatestFeedUpdate)(this.context.getRequestOptionsForCall(requestOptions), new core_sdk_1.EthAddress(owner), new core_sdk_1.Topic(topic));
    }
    /**
     * Validates whether a feed is retrievable in the network.
     *
     * @param owner Owner's ethereum address in hex or bytes
     * @param topic Topic in hex or bytes
     * @param index Optional feed index
     * @param options Download options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async isRetrievable(owner, topic, index, options, requestOptions) {
        const feedOwner = new core_sdk_1.EthAddress(owner);
        const feedTopic = new core_sdk_1.Topic(topic);
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        if (!index) {
            try {
                await this.makeReader(feedTopic, feedOwner, requestOptions).download();
                return true;
            }
            catch (e) {
                const status = cafe_utility_1.Objects.getDeep(e, 'status');
                if (status === 404 || status === 500) {
                    return false;
                }
                throw e;
            }
        }
        return (0, retrievable_1.areAllSequentialFeedsUpdateRetrievable)(this.context.bee, feedOwner, feedTopic, index, options, this.context.getRequestOptionsForCall(requestOptions));
    }
}
exports.Feed = Feed;
