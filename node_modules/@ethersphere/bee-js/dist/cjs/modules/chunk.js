"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Chunk = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const types_1 = require("../types");
const error_1 = require("../utils/error");
const schema_1 = require("../utils/schema");
const api = __importStar(require("../api/chunk"));
/**
 * Chunk operations backed by the `/chunks` endpoint.
 *
 * Accessed as `bee.chunk`.
 */
class Chunk {
    constructor(context) {
        this.context = context;
    }
    /**
     * Uploads a chunk to the network.
     *
     * Chunks uploaded with this method should be retrieved with {@link download}.
     *
     * @param stamp Postage Batch ID or an Envelope created with the `bee.createEnvelope` method.
     * @param data    Raw chunk to be uploaded (Content Addressed Chunk or Single Owner Chunk)
     * @param options Additional options like tag, encryption, pinning, content-type and request options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async upload(stamp, data, options, requestOptions) {
        const isSOC = 'identifier' in data && 'signature' in data && 'owner' in data;
        data = data instanceof Uint8Array ? data : data.data;
        if (options) {
            options = schema_1.UploadOptionsSchema.parse(options);
        }
        if (data.length < core_sdk_1.Span.LENGTH) {
            throw new error_1.BeeArgumentError(`Chunk has to have size of at least ${core_sdk_1.Span.LENGTH}.`, data);
        }
        if (!isSOC && data.length > types_1.CHUNK_SIZE + core_sdk_1.Span.LENGTH) {
            throw new error_1.BeeArgumentError(`Content Addressed Chunk must not exceed ${types_1.CHUNK_SIZE + core_sdk_1.Span.LENGTH} bytes.`, data);
        }
        if (isSOC && data.length > types_1.CHUNK_SIZE + core_sdk_1.Span.LENGTH + core_sdk_1.Signature.LENGTH + core_sdk_1.Identifier.LENGTH) {
            throw new error_1.BeeArgumentError(`Single Owner Chunk must not exceed ${types_1.CHUNK_SIZE + core_sdk_1.Span.LENGTH + core_sdk_1.Signature.LENGTH + core_sdk_1.Identifier.LENGTH} bytes.`, data);
        }
        return api.upload(this.context.getRequestOptionsForCall(requestOptions), data, stamp, options);
    }
    /**
     * Downloads a chunk as a `Uint8Array`.
     *
     * @param reference Bee chunk reference in hex string (either 64 or 128 chars long) or ENS domain.
     * @param options Options that affects the request behavior
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async download(reference, options, requestOptions) {
        const ref = new core_sdk_1.Reference(reference);
        if (options) {
            options = schema_1.DownloadOptionsSchema.parse(options);
        }
        return api.download(this.context.getRequestOptionsForCall(requestOptions), ref, options);
    }
}
exports.Chunk = Chunk;
