"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Collection = void 0;
const core_sdk_1 = require("@ethersphere/core-sdk");
const chunk_stream_1 = require("../utils/chunk-stream");
const collection_1 = require("../utils/collection");
const collection_node_1 = require("../utils/collection.node");
const schema_1 = require("../utils/schema");
const api = __importStar(require("../api/bzz"));
/**
 * Collection (multi-file) operations backed by the `/bzz` endpoint.
 *
 * Accessed as `bee.collection`.
 */
class Collection {
    constructor(context) {
        this.context = context;
    }
    /**
     * Uploads a collection that you assemble yourself.
     *
     * @param postageBatchId
     * @param collection
     * @param options Collection and request options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async upload(postageBatchId, collection, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        (0, collection_1.assertCollection)(collection);
        if (options) {
            options = schema_1.CollectionUploadOptionsSchema.parse(options);
        }
        return api.uploadCollection(this.context.getRequestOptionsForCall(requestOptions), collection, batchId, options);
    }
    /**
     * Uploads a collection of files using the browser `FileList` API.
     *
     * @param postageBatchId Postage BatchId to be used to upload the data with
     * @param fileList list of files to be uploaded
     * @param options Additional options like tag, encryption, pinning and request options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async uploadFromFileList(postageBatchId, fileList, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        if (options) {
            options = schema_1.CollectionUploadOptionsSchema.parse(options);
        }
        const data = (0, collection_1.makeCollectionFromFileList)(fileList);
        return api.uploadCollection(this.context.getRequestOptionsForCall(requestOptions), data, batchId, options);
    }
    /**
     * Uploads a collection of files from a directory on the filesystem.
     *
     * Available only in Node.js as it uses the `fs` module.
     *
     * @param postageBatchId Postage BatchId to be used to upload the data with
     * @param dir the path of the files to be uploaded
     * @param options Additional options like tag, encryption, pinning and request options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async uploadFromDirectory(postageBatchId, dir, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        if (options) {
            options = schema_1.CollectionUploadOptionsSchema.parse(options);
        }
        const data = await (0, collection_node_1.makeCollectionFromFS)(dir);
        return api.uploadCollection(this.context.getRequestOptionsForCall(requestOptions), data, batchId, options);
    }
    /**
     * Uploads a collection of files by streaming them to the Bee node, which supports arbitrary
     * sizes, but may be slower due to uploading chunks one by one.
     *
     * Only intended for the browser environment.
     *
     * @param postageBatchId
     * @param files
     * @param onUploadProgress
     * @param options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async stream(postageBatchId, files, onUploadProgress, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        return (0, chunk_stream_1.streamFiles)(this.context.bee, files, batchId, onUploadProgress, options, this.context.getRequestOptionsForCall(requestOptions));
    }
    /**
     * Uploads a directory by streaming its contents directly to the Bee node, which supports
     * arbitrary directory sizes, but may be slower due to uploading chunks one by one.
     *
     * Only intended for the Node.js environment.
     *
     * @param postageBatchId
     * @param dir
     * @param onUploadProgress
     * @param options
     * @param requestOptions Options for making requests, such as timeouts, custom HTTP agents, headers, etc.
     */
    async streamFromDirectory(postageBatchId, dir, onUploadProgress, options, requestOptions) {
        const batchId = new core_sdk_1.BatchId(postageBatchId);
        return (0, chunk_stream_1.streamDirectory)(this.context.bee, dir, batchId, onUploadProgress, options, this.context.getRequestOptionsForCall(requestOptions));
    }
    /**
     * Hashes a directory locally and returns the root hash (Swarm reference).
     *
     * The actual Swarm reference may be different as there is no canonical hashing of directories.
     *
     * @param dir
     */
    async hashDirectory(dir) {
        return (0, chunk_stream_1.hashDirectory)(dir);
    }
}
exports.Collection = Collection;
